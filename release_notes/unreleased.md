**Unreleased**

* Converted the app to the Splunk SOAR Apps SDK.
* Interactive question responses are now received over a webhook instead of a REST handler. The **Request URL** configured in Slack must be updated to the asset's webhook URL.
